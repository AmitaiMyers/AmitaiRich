name: ntp
channels:
  - defaults
dependencies:
  - python=3.13
  - pip
  - pip:
      - yfinance>=0.2.40
      - streamlit>=1.30
      - plotly>=5.18
      - pandas>=2.0
      - numpy>=1.24
      - fastapi>=0.110
      - uvicorn>=0.29
