# Capital Market Roof Simulator — dependencies
# sqlite3 is part of the Python standard library (no entry needed).
yfinance>=0.2.40
streamlit>=1.30
plotly>=5.18
pandas>=2.0
numpy>=1.24

# Day-trading simulator (sim/) — FastAPI server for the canvas front-end.
fastapi>=0.110
uvicorn>=0.29

# DQN trading agent (sim/agent/). CPU build by default; for GPU install the CUDA
# build instead, e.g.:  pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0
